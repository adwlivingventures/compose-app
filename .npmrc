# Native-only Expo app: expo-router's optional web-support packages declare
# react-dom peer deps we intentionally don't install. Expo CLI ignores this;
# this flag makes plain npm commands behave the same way.
legacy-peer-deps=true
